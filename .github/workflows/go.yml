name: Build

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: PlakarKorp/ci-workflows/.github/workflows/build-go.yml@build-go
    with:
      target: ./...
      test-option-timeout: 30s
    secrets: inherit
